meta {
  name: Delete
  type: http
  seq: 5
}

delete {
  url: {{BASE_URL}}/api/clinical/tumor-types/:id
  body: none
  auth: bearer
}

auth:bearer {
  token: {{BEARER_TOKEN}}
}

params:path {
  id: 1
}

settings {
  encodeUrl: true
  timeout: 0
}

