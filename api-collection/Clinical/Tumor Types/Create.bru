meta {
  name: Create
  type: http
  seq: 1
}

post {
  url: {{BASE_URL}}/api/clinical/tumor-types
  body: json
  auth: bearer
}

auth:bearer {
  token: {{BEARER_TOKEN}}
}

body:json {
  {
    "name": "Cáncer de Mama",
    "systemAffected": "Glándulas"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
