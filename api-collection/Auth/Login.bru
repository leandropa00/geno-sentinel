meta {
  name: Login
  type: http
  seq: 1
}

post {
  url: {{BASE_URL}}/api/auth/login
  body: json
  auth: none
}

body:json {
  {
      "username": "admin",
      "password": "admin123"
  }
}

script:post-response {
  let data = res.body;
  if (data && data.token) {
    bru.setEnvVar("BEARER_TOKEN", data.token);
  }
}

settings {
  encodeUrl: true
}
