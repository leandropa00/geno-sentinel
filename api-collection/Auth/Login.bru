meta {
  name: Login
  type: http
  seq: 1
}

post {
  url: {{BASE_URL}}/api/auth/login
  body: json
  auth: none
}

body:json {
  {
      "username": "admin",
      "password": "admin123"
  }
}

settings {
  encodeUrl: true
}
